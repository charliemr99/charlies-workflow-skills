# Feedback Inbox

The Feedback Inbox presents customer feedback in its established display order.
Each item shows its status, customer, title, and summary. The fixture currently
has no query controls and does not persist view state in the URL.

The application is intentionally local-only. It has no authentication, remote
API, analytics, environment variables, or deployment configuration.
