interface:
  display_name: "Ponytail Audit"
  short_description: "Audit a repository for unnecessary complexity"
  default_prompt: "Use $ponytail-audit to report repo-wide over-engineering without applying fixes."
policy:
  allow_implicit_invocation: false
