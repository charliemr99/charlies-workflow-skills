interface:
  display_name: "Charlie's Workflow"
  short_description: "Feature and greenfield MVP delivery workflow"
  default_prompt: "Use $charlies-workflow to deliver the requested change, automatically selecting the track, risk tier, helpers and verification scope while preserving approval and quality gates."
policy:
  allow_implicit_invocation: false
