interface:
  display_name: "Charlie's Workflow"
  short_description: "Feature and greenfield MVP delivery workflow"
  default_prompt: "Use $charlies-workflow to select Feature or Project Track, complete discovery and approval gates, implement with proportional rigor, preserve only durable documentation, verify the final result, and open a draft PR."
policy:
  allow_implicit_invocation: false
